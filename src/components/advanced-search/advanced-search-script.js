document.addEventListener("DOMContentLoaded", function () {
	var searchLocationPlaceholder = document.getElementsByClassName(
		"css-1wa3eu0-placeholder"
	);

	if (searchLocationPlaceholder.length > 0) {
		searchLocationPlaceholder[0].innerHTML = "Introduceti o locatie...";
	}
});
